# Set default behavior for all files to be text and use LF line endings in the repo.
# This will normalize all text files to LF on commit.
* text=auto

# Explicitly declare text files you want to always have LF line endings in the repo.
*.py text
.env text
.gitignore text